import { TextSeletionDirective } from './text-seletion.directive';

describe('TextSeletionDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSeletionDirective();
    expect(directive).toBeTruthy();
  });
});
